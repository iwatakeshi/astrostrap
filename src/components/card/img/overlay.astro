---
import Box from "../../box";
import type { ComponentProps } from "astro/types";
interface Props extends ComponentProps<typeof Box> {}
const props = Astro.props;
---

<Box class:list={["card-overlay", Astro.props.class]} {...props}>
  <slot />
</Box>
