---
import type { HTMLAttributes } from "astro/types";
interface Props extends HTMLAttributes<"img"> {
  position?: "top" | "bottom";
}

const { position, ...props } = Astro.props as Props;
---

<img class:list={[`card-img${position ? `-${position}` : ""}`]} {...props} />
