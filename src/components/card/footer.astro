---
import Box from "../box";
import type { ComponentProps } from "astro/types";
interface Props extends ComponentProps<typeof Box> {}
---

<Box class:list={["card-footer", Astro.props.class]} {...Astro.props}>
  <slot />
</Box>
