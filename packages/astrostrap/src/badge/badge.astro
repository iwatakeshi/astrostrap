---
import Box from "../box/box.astro";
import type { ComponentProps } from "astro/types";
interface Props extends ComponentProps<typeof Box> {}
const { as: As = "span", ...props } = Astro.props;
---

<As class:list="badge" {...props}>
  <slot />
</As>
